language: elixir
env:
  - MIX_ENV=test
elixir:
  - 1.4.2
otp_release:
  - 17.5
  - 18.0
after_script:
  - mix deps.get --only docs
  - MIX_ENV=docs mix inch.report
after_success:
  - MIX_ENV=test mix deps.get && mix credo
  - MIX_ENV=test mix coveralls.travis

language: elixir
elixir:
  - 1.0.3
  - 1.0.4
otp_release:
  - 17.3
  - 17.4
# Unfortunately this can't be enabled. If enabled kiex will fail periodically.
# sudo: false # according to ecto and plug project will go faster by using containers
cache:
   directories:
     - deps
     - $HOME/vendor/elixir
# install:
#  - git clone git://github.com/rebar/rebar.git
#  - cd rebar && ./bootstrap
#  - export PATH=$PATH:`pwd` && cd ..
# script:
#   - yes | MIX_ENV=test mix deps.get
#   - MIX_ENV=test mix amrita --trace
after_script:
  - mix deps.get --only docs
  - MIX_ENV=docs mix inch.report

dependencies:
  pre:
    - apt-get install erlang, rebar
  cache_directories:
    - otp_src_17.0
    - elixir
test:
  override:
    - erl -version
    - elixir -v
  post:
    - MIX_ENV=test mix local.hex --force
    - MIX_ENV=test mix do clean, deps.get, amrita --trace
